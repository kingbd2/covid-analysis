import session from './session';

const TABLES_END_POINT = 'country/';
const TOTALS_END_POINT = 'totals/';
const CONTINENT_END_POINT = 'continent/';
const COUNTRY_END_POINT = 'country/';
const PROVINCE_STATE_END_POINT = 'province_state/';


// const getAllContinents = () => session.get(CONTINENT_END_POINT);

// you can pass arguments to use as request parameters/data
// const getTable = (table_name) => session.get(TABLES_END_POINT, {
//   table_name
// });
// maybe more than one..
// const createUser = (username, password) => httpClient.post(END_POINT, {
//   username,
//   password
// });

export default {
//   getAllContinents,
}
